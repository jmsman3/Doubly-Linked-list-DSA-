#include <bits/stdc++.h>
using namespace std;
int main()
{
    list<int> MyList = {16,10, 30, 50, 40, 20, 50};
    // MyList.remove(10);
    // MyList.sort();
    // MyList.sort(greater<int>());
    // MyList.unique();
    // MyList.reverse();
     cout<< *next(MyList.begin(), 4 )<<endl;

    // for (int val : MyList)
    // {
    //     cout << val << endl;
    // }
    // return 0;
}